language: node_js

# safelist
branches:
  only:
  - master
  - travis

before_install:
  - cd frontend
  - npm install
  - npm install -g tsc typings
  - typings install

install:
  - cd frontend
  - tsc

script:
  - cd frontend
  - ./node_modules/karma/bin/karma start --single-run=true

